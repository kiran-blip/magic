# ── Gold Digger (JARVIS v4) Environment Variables ────────────────────
#
# Copy this file to .env.local (for local dev) or set these in Railway dashboard.
#
# For Railway deployment:
#   1. Go to your Railway project → Variables tab
#   2. Add the required variables below
#   3. (Optional) Add a Railway Volume mounted at /app/data for persistent config

# ── Required: At least one LLM API key ──────────────────────────────

# Anthropic API key (for Claude — Premium tier)
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenRouter API key (for Llama models — Light & Standard tiers)
# Get one at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# ── Required: Authentication ─────────────────────────────────────────

# JWT secret for Magic auth (generate with: openssl rand -base64 32)
JWT_SECRET=

# ── Optional: Gold Digger Preferences ────────────────────────────────

# Routing mode: auto | hybrid | anthropic_only | openrouter_only
# GOLDDIGGER_ROUTING=auto

# Feature toggles (true/false)
# GOLDDIGGER_DISCLAIMERS=true
# GOLDDIGGER_SAFETY=true
# GOLDDIGGER_PERSONALITY=true

# Config storage directory (defaults to ./data locally, /app/data in Docker)
# Set this to your Railway Volume mount path if using persistent storage
# GOLDDIGGER_DATA_DIR=/app/data

# ── Optional: Gmail Integration ──────────────────────────────────────

# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GMAIL_REDIRECT_URI=https://your-railway-domain.up.railway.app/api/email/auth/callback

# ── Optional: Local LLM (Ollama) ────────────────────────────────────

# OPENCLAW_API_URL=http://localhost:11434
